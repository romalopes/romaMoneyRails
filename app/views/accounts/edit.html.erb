<% provide(:title, "Edit and Account") %>
<h1>Update an account</h1>
<div class="row">
  <aside class="span4">
      <%= render 'shared/user_summary' %>  
  </aside>
  <div class="span6">
    <%= form_for(@account) do |f| %>
      <%= render 'fields_account', f: f %>

      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>